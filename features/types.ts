// type of date
const today = new Date();
today.getMonth();

// type of string
const coco = "hi";
coco.endsWith();

//
const person = {
  age: 20,
};

person.age;
class Color {}

const red = new Color();

// Type of todo
interface Todos {
  id: string;
  completed: boolean;
}

let haha: Todos;
haha.completed;
